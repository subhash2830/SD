1) if FA is 0.0.0.0then external routes are reachable via finding SPT to ABR

This may lead to sub optimal routing in case of Broadcast segment ( On lan )

to get Optimal path forwarding FA changed to non zero ospf enable non passive interface ( Broadcast only ) address 

