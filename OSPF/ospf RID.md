ospf RID 

its just 32 bit IP address ( Need not to be reachable in ospf domain )

its acts as Node in graph of ospf tree ? Database

Once you reach to Nodes in graph , you can reach its links attributes ( here prefixs )

Thus ospf is routing through nodes


==OSPF RID Selection==
		1.  Manual configuration of the router ID.
		2.  Highest IP address on a loopback interface.
		3.  Highest IP address on a non-loopback interface